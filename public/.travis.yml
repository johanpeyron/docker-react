language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t blixtbild/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true blixtbild/docker-react npm run test
